diff --git a/toolkit/components/search/SearchService.sys.mjs b/toolkit/components/search/SearchService.sys.mjs
index 62f96d71142b7aba06a93c201ea072f579fa7809..90a61cfb22a9b825318299c0b2b0d7d163637f52 100644
--- a/toolkit/components/search/SearchService.sys.mjs
+++ b/toolkit/components/search/SearchService.sys.mjs
@@ -2793,9 +2793,46 @@ export const SearchService = new (class SearchService {
       this._settings.setMetaDataAttribute(key, value);
     }
 
-    return this.#engineSelector.fetchEngineConfiguration(
+    let refinedConfig = await this.#engineSelector.fetchEngineConfiguration(
       searchEngineSelectorProperties
     );
+
+    return this.#enforceAstiangoOnlyConfiguration(refinedConfig);
+  }
+
+  #enforceAstiangoOnlyConfiguration(refinedConfig) {
+    const forcedEngineId = "astiango";
+
+    let astiangoEngine = refinedConfig.engines.find(
+      engine => engine.identifier == forcedEngineId
+    );
+
+    if (!astiangoEngine) {
+      lazy.logConsole.warn(
+        "AstianGO was not present in refined search config; injecting built-in fallback configuration."
+      );
+      astiangoEngine = {
+        identifier: forcedEngineId,
+        name: "AstianGO",
+        aliases: ["astiango", "@astiango"],
+        classification: "general",
+        urls: {
+          search: {
+            base: "https://astiango.com/search",
+            searchTermParamName: "q",
+          },
+          suggestions: {
+            base: "https://astiango.com/autocomplete",
+            searchTermParamName: "q",
+          },
+        },
+      };
+    }
+
+    refinedConfig.engines = [astiangoEngine];
+    refinedConfig.appDefaultEngineId = forcedEngineId;
+    refinedConfig.appPrivateDefaultEngineId = forcedEngineId;
+    return refinedConfig;
   }
 
   #setDefaultFromSelector(refinedConfig) {
@@ -3995,11 +4032,41 @@ class SearchDefaultOverrideAllowlistHandler {
     let searchProvider =
       extension.manifest.chrome_settings_overrides.search_provider;
 
+    // Normalize legacy providers that embed query parameters directly in
+    // `search_url` so comparisons remain stable across format migrations.
+    const normalizeProvider = provider => {
+      let normalized = {
+        ...provider,
+      };
+
+      if (
+        normalized.search_url &&
+        !normalized.search_url_get_params &&
+        normalized.search_url.includes("?")
+      ) {
+        let [base, query] = normalized.search_url.split("?", 2);
+        if (query) {
+          normalized.search_url = base;
+          normalized.search_url_get_params = query;
+        }
+      }
+
+      return normalized;
+    };
+
+    searchProvider = normalizeProvider(searchProvider);
+
     return entry.urls.some(
-      e =>
-        searchProvider.search_url == e.search_url &&
-        searchProvider.search_url_get_params == e.search_url_get_params &&
-        searchProvider.search_url_post_params == e.search_url_post_params
+      e => {
+        const normalizedEntry = normalizeProvider(e);
+        return (
+          searchProvider.search_url == normalizedEntry.search_url &&
+          searchProvider.search_url_get_params ==
+            normalizedEntry.search_url_get_params &&
+          searchProvider.search_url_post_params ==
+            normalizedEntry.search_url_post_params
+        );
+      }
     );
   }
 
