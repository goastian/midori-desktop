diff --git a/browser/components/customizableui/CustomizableUI.sys.mjs b/browser/components/customizableui/CustomizableUI.sys.mjs
index 6d1885a54815d10b3a4e79f7e5acd61ab3abb159..d01c985a9dc564bff1840584693c4b235bf335c4 100644
--- a/browser/components/customizableui/CustomizableUI.sys.mjs
+++ b/browser/components/customizableui/CustomizableUI.sys.mjs
@@ -17,6 +17,7 @@ ChromeUtils.defineESModuleGetters(lazy, {
   PanelMultiView:
     "moz-src:///browser/components/customizableui/PanelMultiView.sys.mjs",
   PrivateBrowsingUtils: "resource://gre/modules/PrivateBrowsingUtils.sys.mjs",
+  ExtensionCommon: "resource://gre/modules/ExtensionCommon.sys.mjs",
   ShortcutUtils: "resource://gre/modules/ShortcutUtils.sys.mjs",
 });
 
@@ -27,6 +28,7 @@ ChromeUtils.defineLazyGetter(lazy, "gWidgetsBundle", function () {
 });
 
 const kDefaultThemeID = "default-theme@mozilla.org";
+const kMidoriPrivacyID = "midori-protection@astian.org";
 
 const kSpecialWidgetPfx = "customizableui-special-";
 
@@ -68,7 +70,7 @@ const kSubviewEvents = ["ViewShowing", "ViewHiding"];
  * The current version. We can use this to auto-add new default widgets as necessary.
  * (would be const but isn't because of testing purposes)
  */
-var kVersion = 26;
+var kVersion = 27;
 
 /**
  * Buttons removed from built-ins by version they were removed. kVersion must be
@@ -367,6 +369,7 @@ var CustomizableUIInternal = {
         : "home-button",
       "spring",
       "vertical-spacer",
+      `${lazy.ExtensionCommon.makeWidgetId(kMidoriPrivacyID)}-browser-action`,
       "urlbar-container",
       "spring",
       "downloads-button",
@@ -924,6 +927,18 @@ var CustomizableUIInternal = {
         );
       }
     }
+
+    if (currentVersion < 27) {
+      const privacyButtonId = `${lazy.ExtensionCommon.makeWidgetId(kMidoriPrivacyID)}-browser-action`;
+      const navbarPlacements =
+        gSavedState.placements[CustomizableUI.AREA_NAVBAR];
+      if (navbarPlacements && !navbarPlacements.includes(privacyButtonId)) {
+        const urlbarIndex = navbarPlacements.indexOf("urlbar-container");
+        if (urlbarIndex !== -1) {
+          navbarPlacements.splice(urlbarIndex, 0, privacyButtonId);
+        }
+      }
+    }
   },
 
   /**
