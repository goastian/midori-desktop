diff --git a/browser/components/preferences/search.js b/browser/components/preferences/search.js
index d8a36e15da281872d4be9a8d46176ad785f8c82d..9d39523912620f06f038cff072eb0ceecd67c6bb 100644
--- a/browser/components/preferences/search.js
+++ b/browser/components/preferences/search.js
@@ -48,7 +48,8 @@ var gSearchPane = {
      init() {
            initSettingGroup("defaultEngine");
            initSettingGroup("searchSuggestions");
            initSettingGroup("firefoxSuggest");
+    this.disableSearchEngineEditingUI();
            this._engineStore = new EngineStore();
            gEngineView = new EngineView(this._engineStore);
 
@@ -72,8 +73,34 @@ var gSearchPane = {
            // Access the lazy preferences so that updates will be observed.
            lazy.separatePrivateDefaultEnabledPrefValue;
            lazy.separatePrivateDefaultPrefValue;
      },
 
+  disableSearchEngineEditingUI() {
+    let defaultEngine = document.getElementById("defaultEngine");
+    if (defaultEngine) {
+      defaultEngine.disabled = true;
+    }
+
+    let defaultPrivateEngine = document.getElementById("defaultPrivateEngine");
+    if (defaultPrivateEngine) {
+      defaultPrivateEngine.disabled = true;
+    }
+
+    let separatePrivateToggle = document.getElementById(
+      "browserSeparateDefaultEngine"
+    );
+    if (separatePrivateToggle) {
+      separatePrivateToggle.disabled = true;
+    }
+
+    let oneClickSearchProvidersGroup = document.getElementById(
+      "oneClickSearchProvidersGroup"
+    );
+    if (oneClickSearchProvidersGroup) {
+      oneClickSearchProvidersGroup.hidden = true;
+    }
+  },
+
      // ADDRESS BAR
      handleEvent(aEvent) {
            if (aEvent.type != "command") {
