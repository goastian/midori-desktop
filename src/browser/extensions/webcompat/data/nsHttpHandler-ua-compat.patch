diff --git a/netwerk/protocol/http/nsHttpHandler.cpp b/netwerk/protocol/http/nsHttpHandler.cpp
index c45cb9f041962918449b31aaff0a9d435dea06b1..de878ff47e322c6f727b5a84a8f923b0c384861c 100644
--- a/netwerk/protocol/http/nsHttpHandler.cpp
+++ b/netwerk/protocol/http/nsHttpHandler.cpp
@@ -1069,8 +1069,11 @@ void nsHttpHandler::BuildUserAgent() {
     mUserAgent += ' ';
     mUserAgent += mCompatFirefox;
   }
-  if (!isFirefox) {
-    // App portion
+  if (!isFirefox && !mCompatFirefoxEnabled) {
+    // App portion — only append when NOT in Firefox compat mode.
+    // Midori: When compatMode.firefox is enabled, we produce a pure
+    // Firefox UA to avoid UA-sniffing breakage on sites like Netflix,
+    // reCAPTCHA, and their subdomains/CDNs.
     mUserAgent += ' ';
     mUserAgent += mAppName;
     mUserAgent += '/';
