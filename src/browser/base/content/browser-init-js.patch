diff --git a/browser/base/content/browser-init.js b/browser/base/content/browser-init.js
index 7694c598c35dc464fbdd8290d8a4d2c00c84acd4..0aba588724d3e620ebeef4945cb8282d6d3c54a7 100644
--- a/browser/base/content/browser-init.js
+++ b/browser/base/content/browser-init.js
@@ -502,6 +502,16 @@ var gBrowserInit = {
 
     UpdateUrlbarSearchSplitterState();
 
+    // Midori: materialize Firefox's built-in bookmark action after page-action
+    // initialization so the star is never lost from the URL bar.
+    {
+      const bookmarkAction = PageActions.actionForID("bookmark");
+      if (bookmarkAction) {
+        BrowserPageActions.placeActionInUrlbar(bookmarkAction);
+      }
+      BookmarkingUI.updateStarState();
+    }
+
     let safeMode = document.getElementById("helpSafeMode");
     if (Services.appinfo.inSafeMode) {
       document.l10n.setAttributes(safeMode, "menu-help-exit-troubleshoot-mode");
