diff --git a/browser/modules/FaviconLoader.sys.mjs b/browser/modules/FaviconLoader.sys.mjs
index 10f8044a248b692368da1b868b9b9597c611be5c..ee336d8f1ea4ae0e44e4ecb060389eaf69244af7 100644
--- a/browser/modules/FaviconLoader.sys.mjs
+++ b/browser/modules/FaviconLoader.sys.mjs
@@ -433,7 +433,14 @@ function extractIconSize(aSizes) {
  */
 function getLinkIconURI(aLink) {
   let targetDoc = aLink.ownerDocument;
-  let uri = Services.io.newURI(aLink.href, targetDoc.characterSet);
+  let uri;
+  try {
+    uri = Services.io.newURI(aLink.href, targetDoc.characterSet);
+  } catch (e) {
+    // Malformed favicon href (e.g. empty, bad scheme, or otherwise invalid).
+    // Returning null lets callers gracefully skip this icon.
+    return null;
+  }
   try {
     uri = uri.mutate().setUserPass("").finalize();
   } catch (e) {
